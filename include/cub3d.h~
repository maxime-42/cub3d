#ifndef CUB3D_H
# define CUB3D_H
#include <stdio.h>
#include "../libft/libft.h"

typedef struct	    s_control
{
	char	*tab[3];
}		       t_control;			     

typedef struct		s_node
{
	char		*line;
	struct s_list	*next;
}			       t_node;
#endif
