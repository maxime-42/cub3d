#include "cub3d.h"


int	keyPressed(t_info *info)
{
	/* t_player *player; */

	/* player = &info->player; */
	/* if (info->keyCode == UP_ARROW) */
	/* { */
	/* 	player->walkDirection += 1; */
	/* } */
	/* else if (info->keyCode == DOW_ARROW) */
	/* { */
	/* 	player->walkDirection -= 1; */
	/* } */
	/* else if (info->keyCode == LEFT_ARROW) */
	/* { */
	/* 	player->turnDirection -= 1; */
	/* 	printf("Left\n"); */
	/* } */
	/* else if (info->keyCode == RIGHT_ARROW) */
	/* { */
	/* 	player->turnDirection += 1; */
	/* 	printf("Right\n"); */
	/* } */
	/* else if (info->keyCode == QUIT) */
	/* { */
	/* 	exit(0); */
	/* } */
	/* if (info->keyCode == LEFT_ARROW || info->keyCode == RIGHT_ARROW) */
	/* { */
	/* 	printf("player->x == %d\n", player->x); */
	/* 	printf("player->y == %d\n", player->y); */
	/* 	/\* player->turnDirection = 0; *\/ */
	/* /\* player->rotationAngle += player->turnDirection * player->rotationSpeed; *\/ */

	/* } */
	(void)info;
	/* (void)player; */
	return (0);
}

/* int		draw(t_info *info) */
/* { */
/* 	if (setup(info) == ERROR) */
/* 	{ */
/* 		return (ERROR); */
/* 	} */
/* 	renderMap((char **)info->map, &info->pixel); */
/* 	info->player.x = 10; */
/* 	info->player.y = 10; */
/* 	mlx_key_hook(info->pixel.win_mlx, &keyPressed, info); */
/* 	return (SUCCESS); */
/* } */
